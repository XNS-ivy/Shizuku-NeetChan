export default {
    name: 'neet',
    desc: 'role playing with your beloved loli',
    premium: false,
    execute: async ({ msg, args }) => {
        const mongo_id = msg.id.split('@')[0]
        const menu = args[0]
        switch (menu) {
            case 'sex':

                break
            case 'talk':

                break

            case 'stat':
            
                break
            default:
                break
        }
    }
}